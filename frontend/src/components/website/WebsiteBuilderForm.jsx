import { useState } from 'react';
export default function WebsiteBuilderForm({ onSubmit }) { const [form, setForm] = useState({ tier:'pro', business_name:'', offer:'', ideal_customer:'', website_goal:'lead_capture', domain_idea:'', tone:'', services:'' }); return <form className='space-y-3' onSubmit={(e)=>{e.preventDefault();onSubmit({...form, services: form.services?form.services.split(',').map(s=>s.trim()):[]});}}>{Object.entries(form).map(([k,v])=><input key={k} className='w-full p-2 bg-slate-900 border border-blue-500 rounded' placeholder={k} value={v} onChange={e=>setForm({...form,[k]:e.target.value})} />)}<button className='bg-amber-500 px-4 py-2 rounded font-bold'>Generate Website Builder</button></form>; }
